#!/system/bin/sh

export PATH=/usr/sbin:/usr/bin:/system/xbin:/system/sbin:/system/bin:/sbin
for i in /system/etc/init.d/*; do
    if [ -x $i ]; then
        /system/bin/log -t sysinit Running $i
        $i
    fi
done
